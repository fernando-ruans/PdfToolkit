{
  "name": "pdf-toolkit-monolith",
  "private": true,
  "scripts": {
    "install": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1",
    "react-pdf": "^10.0.1",
    "react-router-dom": "^7.8.0"
  }
}
